# Browser-facing REST API origin (no trailing slash).
VITE_API_BASE_URL=http://localhost:8787

# WebSocket endpoint. Use wss:// in production.
VITE_WS_URL=ws://localhost:8787

# Optional realtime tuning (milliseconds).
VITE_WS_HEARTBEAT_INTERVAL_MS=25000
VITE_WS_STALE_AFTER_MS=15000
